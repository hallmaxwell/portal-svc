services:
  sing-box:
    build: .
    container_name: sing-box
    restart: always
    network_mode: host
    env_file: .env
    volumes:
      - ./config.json:/config.json
      - /etc/localtime:/etc/localtime:ro